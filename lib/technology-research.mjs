import jsdomPackage from 'jsdom';
import { fetchRssSource, rssXml } from './rss-merge.mjs';
import { setAsciiHeader } from './http-response-safe.mjs';

const { JSDOM, VirtualConsole } = jsdomPackage;
const CACHE_TTL = 6 * 60 * 60 * 1000;
const memoryCache = { at: 0, xml: '', items: [], meta: null };
const RESEARCH_VERSION = '生産技術8タブ-v6';
const RESEARCH_CATEGORIES = ['生技基礎', '改善事例', '技術革新', '論文・研究', '製品・製造技術', '異業種横展開'];
const WEB_CATEGORIES = ['生技基礎', '改善事例', '技術革新', '製品・製造技術', '異業種横展開'];
const WEB_REQUESTS_PER_REFRESH = 15;

const PRODUCTION_RE = /(生産技術|製造技術|ものづくり|モノづくり|生産性|生産効率|工程改善|工程設計|工程管理|現場改善|省人化|省力化|自動化|自働化|標準作業|作業標準|標準工数|industrial\s*engineering|\bIE\b|ラインバランス|line\s*balanc|タクト|takt|cycle\s*time|サイクルタイム|セル生産|コンベア生産|一個流し|多品種少量|変種変量|段取り|SMED|ボトルネック|仕掛|\bWIP\b|かんばん|kanban|andon|アンドン|\bMES\b|製造DX|スマートファクトリ|工場DX|生産計画|人員配置|作業改善|工程|工場|製造現場)/i;
const QUALITY_RE = /(\bQCD\b|\bSQCD\b|品質管理|品質保証|品質改善|品質向上|不良率|不良低減|不良削減|歩留まり|直行率|\bFPY\b|first\s*pass\s*yield|工程能力|\bCp\b|\bCpk\b|\bSPC\b|ポカヨケ|poka[ -]?yoke|外観検査|画像検査|検査自動化|測定|再加工|手直し|原価低減|コスト削減|材料ロス|リードタイム|納期|品質)/i;
const EQUIPMENT_RE = /(\bOEE\b|設備総合効率|総合設備効率|設備効率|稼働率|可動率|稼動率|停止ロス|停止時間|チョコ停|微停止|設備停止|設備保全|予防保全|予知保全|予兆保全|\bTPM\b|\bMTBF\b|\bMTTR\b|故障解析|保全計画|ダウンタイム|設備)/i;
const TRACE_RE = /(トレーサビリティ|traceability|\b4M\b|ロット追跡|lot\s*trace|Lot\s*No|履歴管理|工程履歴|検査履歴|部品情報|製造実績|設備データ|稼働データ|\bRFID\b|QRコード|二次元コード|\bPLC\b|OPC\s*UA|個体管理|ロット管理|製造履歴)/i;
const FOUNDATION_RE = /(基礎|基本|入門|とは|考え方|原理|手法|計算式|算出方法|計算方法|使い分け|特徴|メリット|デメリット|標準時間|工程分析|動作研究|動作分析|時間研究|稼働分析|方法工学|作業測定|\bECRS\b|QC7つ道具|なぜなぜ分析|5S|標準作業|ラインバランス|ラインバランシング|セル生産|\bQCD\b|\bOEE\b|\bIE\b|ワークサンプリング|VSM|工程能力|\bCp\b|\bCpk\b|\bSPC\b|\bTPM\b|\bMTBF\b|\bMTTR\b)/i;
const CASE_RE = /(導入|改善|活用事例|導入事例|改善事例|事例|ケーススタディ|case\s*study|実証|実践|課題|解決策|改善前|改善後|before\s*after|削減|短縮|向上|低減|\d+(?:\.\d+)?\s*%|工数|時間\/日|円\/年|万円|人\/|名\/|停止時間|省人|省力|効率化)/i;
const INNOVATION_RE = /(新技術|技術革新|新工法|新製法|新素材|新構造|世界初|業界初|独自技術|革新|innovation|novel|breakthrough|新規開発|開発した|実用化|量産化|研究開発|R&D|デジタルツイン|AI|機械学習|画像認識|センシング|ロボット|協働ロボット|AMR|3Dプリンタ|積層造形|形式知化|スマートファクトリ|予知保全|予兆保全)/i;
const PRODUCT_RE = /(炊飯|米飯|rice\s*cook|rice\s*cooker|真空断熱|vacuum\s*(?:insulation|flask|bottle)|魔法瓶|thermos|電気ケトル|kettle|電気ポット|water\s*(?:boiler|warmer)|保温|保冷|heat\s*retention|temperature\s*control|温度制御|沸騰|boil|加熱|heating|thermal|熱伝達|断熱|insulation|phase\s*change|蓄熱|heat\s*storage|蒸気|steam|コーティング|coating|接合|joining|溶接|ろう付け|センサ|sensor|洗浄|cleaning|表面処理|材料技術|成形|組立|ヒーター|誘導加熱|IH)/i;
const BRAND_RE = /(タイガー魔法瓶|象印|zojirushi|panasonic|パナソニック|thermos|サーモス|iris\s*ohyama|アイリスオーヤマ|balmuda|バルミューダ|vermicular|バーミキュラ|siroca|山善|fellow|breville|de['’]?longhi|hydro\s*flask|stanley|ember)/i;
const CROSS_INDUSTRY_RE = /(自動車|自動車部品|automotive|半導体|semiconductor|電子部品|\bEMS\b|食品工場|食品製造|food\s*factory|医薬|製薬|pharma|化学工場|物流|logistics|倉庫|航空|aerospace|機械加工|金属加工|射出成形|樹脂成形|組立工場|包装|packaging|製鉄|鉄鋼|電池|battery|建機|工作機械)/i;
const PRACTICAL_RE = /(方法|手順|進め方|仕組み|原理|計算|算出|比較|指標|KPI|効果|課題|原因|対策|注意点|失敗|改善|導入|実装|検証|評価|実験|測定|解析|可視化|見える化|設計|標準化|定量)/i;
const EFFECT_RE = /(\d+(?:\.\d+)?\s*(?:%|％|秒|分|時間|円|万円|人|名)|削減|短縮|向上|低減|改善前|改善後|before|after)/i;
const PROMO_ONLY_RE = /(キャンペーン|セール|割引|プレゼント|ランキング|おすすめ\d+選|購入はこちら|予約販売|クーポン|新発売のお知らせ|発売記念|販売開始|価格改定|採用情報|求人|セミナー申込|資料ダウンロードだけ)/i;
const BLOCKED_DOMAIN_RE = /(?:^|\.)(?:prtimes\.jp|businesswire\.com|prnewswire\.com|makuake\.com|camp-fire\.jp|kickstarter\.com)$/i;
const TRUSTED_PRIMARY_RE = /(?:j-ie\.com|jmac\.co\.jp|omron\.|keyence\.|mitsubishielectric\.co\.jp|panasonic\.|aist\.go\.jp|nedo\.go\.jp|jst\.go\.jp|jstage\.jst\.go\.jp|cir\.nii\.ac\.jp|itmedia\.co\.jp|monoist|ricoh\.|canon\.|fujifilm\.|daikin\.|yaskawa\.|fanuc\.|denso\.|toyota\.)/i;

const PAPER_QUERIES = ['生産技術 IE 生産性','工程改善 ライン 生産システム','品質管理 工程能力 歩留まり','設備保全 OEE 稼働率','トレーサビリティ MES 製造','熱伝達 断熱 加熱 温度制御'];
const SEARCH_PLANS = {
  '生技基礎': [
    { query: '製造業 生産技術 基礎 IE OEE 標準時間 工程分析 動作分析 ECRS' },
    { query: 'セル生産 ラインバランス タクト Cp Cpk SPC TPM MTBF MTTR トレーサビリティ MES 製造' },
    { query: '生産技術 IE OEE セル生産 タクト 工程分析 品質管理 トレーサビリティ', includeDomains: ['note.com'], noteOnly: true }
  ],
  '改善事例': [
    { query: '工場 改善事例 生産性 タクト 段取り OEE 省人化 ライン改善' },
    { query: '製造 品質改善 不良 歩留まり トレーサビリティ MES 予防保全 改善事例' },
    { query: '生産技術 改善 タクト 設備 OEE 品質 省人化 治具 トレーサビリティ', includeDomains: ['note.com'], noteOnly: true }
  ],
  '技術革新': [
    { query: '製造 新技術 AI 外観検査 センシング デジタルツイン 協働ロボット AMR' },
    { query: '製造 新工法 新素材 接合 コーティング 3Dプリンタ 予知保全 技術開発' },
    { query: '製造 生産技術 新技術 AI ロボット センサ 新工法 予知保全', includeDomains: ['note.com'], noteOnly: true }
  ],
  '製品・製造技術': [
    { query: '炊飯器 魔法瓶 真空断熱 電気ケトル 加熱 温度制御 保温 蒸気 技術' },
    { query: '家電 製造技術 熱伝達 断熱 接合 溶接 コーティング 洗浄 センサ 成形 組立' },
    { query: '家電 製品開発 製造技術 断熱 加熱 温度制御 接合 コーティング 洗浄', includeDomains: ['note.com'], noteOnly: true }
  ],
  '異業種横展開': [
    { query: '自動車 半導体 電子部品 工場 改善 OEE 品質 トレーサビリティ 自動化' },
    { query: '食品 医薬 物流 鉄鋼 機械加工 樹脂成形 工場 生産性 改善事例 省人化' },
    { query: '自動車 半導体 食品 物流 工場 改善 生産技術 品質 OEE トレーサビリティ', includeDomains: ['note.com'], noteOnly: true }
  ]
};

const DIRECT_SEEDS = [
  {category:'生技基礎',sourceLabel:'日本IE協会',organization:'日本インダストリアル・エンジニアリング協会',title:'IEとは',url:'https://www.j-ie.com/about_ie/',publishedAt:'2024-02-26',summary:'IE（Industrial Engineering）の基本的な考え方を解説。人・モノ・情報・設備・エネルギーを含むシステムを設計・改善し、価値とムダを定量的に捉えて生産性向上につなげる基礎。'},
  {category:'生技基礎',sourceLabel:'日本IE協会',organization:'日本インダストリアル・エンジニアリング協会',title:'動作研究',url:'https://www.j-ie.com/about_ie/methods/01/',publishedAt:'2024-02-19',summary:'作業者の身体動作や目の動きを分析し、ムダな動作をなくして効率的で疲労の少ない作業方法を確立するIE手法。サーブリッグ分析など改善の基礎を体系的に解説。'},
  {category:'生技基礎',sourceLabel:'日本IE協会',organization:'日本インダストリアル・エンジニアリング協会',title:'時間研究',url:'https://www.j-ie.com/about_ie/methods/02/',publishedAt:'2024-03-15',summary:'作業を要素作業に分解して時間を測定し、作業改善や標準時間設定につなげる時間研究の基礎。改善効果を時間で定量評価する考え方を解説。'},
  {category:'生技基礎',sourceLabel:'日本IE協会',organization:'日本インダストリアル・エンジニアリング協会',title:'稼働分析',url:'https://www.j-ie.com/about_ie/methods/03/',publishedAt:'2024-03-15',summary:'作業者や設備の稼働率と時間構成比率を求め、稼働・準稼働・非稼働に層別してムダを定量化するIE手法。連続観測法とワークサンプリングも解説。'},
  {category:'生技基礎',sourceLabel:'日本IE協会',organization:'日本インダストリアル・エンジニアリング協会',title:'工程分析',url:'https://www.j-ie.com/about_ie/methods/04/',publishedAt:'2024-03-15',summary:'材料や製品、作業者、運搬の流れを記号化して工程全体を可視化し、ムダや改善対象を見つける工程分析の基本を解説。'},
  {category:'生技基礎',sourceLabel:'日本IE協会',organization:'日本インダストリアル・エンジニアリング協会',title:'標準時間',url:'https://www.j-ie.com/about_ie/term/04/',publishedAt:'2024-03-15',summary:'標準時間を作業管理・生産計画・標準原価の基準として使う考え方と、正味時間・余裕時間・レイティング係数を用いた基本計算を解説。'},
  {category:'生技基礎',sourceLabel:'日本IE協会',organization:'日本インダストリアル・エンジニアリング協会',title:'流動数分析',url:'https://www.j-ie.com/about_ie/methods/11/',publishedAt:'2024-03-15',summary:'工程への累積流入量と累積流出量の差から仕掛品在庫量や停滞時間を把握し、生産進捗やWIPのムダを分析するIE手法。'},
  {category:'生技基礎',sourceLabel:'OMRON FA',organization:'オムロン',title:'OEEとは？7大ロスを見直して設備総合効率を改善',url:'https://www.fa.omron.co.jp/product/special/maintenance-solution/column/column10/',publishedAt:'2025-01-01',summary:'OEE（設備総合効率）を時間稼働率×性能稼働率×良品率で算出し、停止・性能・不良などのロスへ分解して設備改善につなげる方法を解説。'},
  {category:'生技基礎',sourceLabel:'JMAC',organization:'日本能率協会コンサルティング',title:'IE・標準時間設定コンサルティング',url:'https://www.jmac.co.jp/consulting/category/production/most.html',publishedAt:'2025-01-01',summary:'方法改善、作業分析、稼働分析、工程分析、ライン作業分析、標準時間設定など、IEの基礎テクニックと現場改善の進め方を体系的に整理。'},
  {category:'生技基礎',sourceLabel:'JMAC',organization:'日本能率協会コンサルティング',title:'工程分析',url:'https://www.jmac.co.jp/glossary/ka/process_analysis.html',publishedAt:'2025-01-01',summary:'材料や製品の工程系列を記号で表し、工程構成・相互関係・問題点を明確化して、生産期間短縮、仕掛削減、工数低減につなげる工程分析の基礎。'},
  {category:'改善事例',sourceLabel:'OMRON FA',organization:'オムロン',title:'現場と経営の情報連携プラットフォームを専門的なIT知識不要で構築',url:'https://www.fa.omron.co.jp/solution/proposal/app_043/',publishedAt:'2025-01-01',summary:'工場全体のOEEと各ラインの稼働データをひも付け、問題設備の特定やKPI可視化を属人作業から切り離して改善サイクルへつなげる事例。'},
  {category:'改善事例',sourceLabel:'OMRON FA',organization:'オムロン',title:'トレーサビリティで実現する変種変量生産の品質管理',url:'https://www.fa.omron.co.jp/solution/proposal/app_006/',publishedAt:'2025-01-01',summary:'製品と部品単位に4M情報、工程・作業情報をひも付けて品質を管理し、不良流出時の影響範囲特定や作業ミス防止へつなげるトレーサビリティ事例。'},
  {category:'技術革新',sourceLabel:'OMRON FA',organization:'オムロン',title:'熟練技能者が持つ匠の技を形式知化。設備総合効率をさらに向上',url:'https://www.fa.omron.co.jp/solution/proposal/app_001/',publishedAt:'2025-01-01',summary:'AI・IoT・センシングを使って熟練者の暗黙知を形式知化し、停止ロス、段取りロス、チョコ停、不良ロスなどOEE阻害要因を改善する技術アプローチ。'}
];

function clean(value='',max=2400){return String(value||'').replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ').replace(/&nbsp;/gi,' ').replace(/&amp;/gi,'&').replace(/\s+/g,' ').trim().slice(0,max);}
function safeUrl(value=''){const input=String(value||'').trim();const match=input.match(/https?:\/\/[^\s<>"')\]】]+/i);const candidate=(match?.[0]||input).replace(/[.,、。!！?？;；:：]+$/g,'').trim();try{const url=new URL(candidate);return /^https?:$/.test(url.protocol)?url.href:'';}catch{return '';}}
function hostOf(value=''){try{return new URL(value).hostname.replace(/^www\./,'').toLowerCase();}catch{return '';}}
function normalizeDate(value=''){if(value instanceof Date&&Number.isFinite(value.getTime()))return value;const date=new Date(value||'');return Number.isFinite(date.getTime())?date:new Date(0);}
function itemKey(item={}){const url=String(item.url||'').replace(/[?#].*$/,'').toLowerCase();if(url)return `url:${url}`;return `title:${String(item.title||'').replace(/\s+/g,' ').trim().toLowerCase()}`;}
function dedupe(items=[]){const seen=new Set();return items.filter(item=>{if(!item)return false;const key=itemKey(item);if(!key||seen.has(key))return false;seen.add(key);return true;});}
function isBlockedUrl(url=''){return BLOCKED_DOMAIN_RE.test(hostOf(url));}
function scoreOf(value){const score=Number(value);return Number.isFinite(score)?Math.max(0,Math.min(100,score)):0;}
function localTopicRelevant(text=''){return PRODUCTION_RE.test(text)||QUALITY_RE.test(text)||EQUIPMENT_RE.test(text)||TRACE_RE.test(text)||PRODUCT_RE.test(text);}
function categoryOf(raw={}){const explicit=clean(raw.category,80);if(RESEARCH_CATEGORIES.includes(explicit))return explicit;const hay=`${raw.title||''} ${raw.summary||raw.description||''}`;if(CROSS_INDUSTRY_RE.test(hay)&&localTopicRelevant(hay))return '異業種横展開';if(PRODUCT_RE.test(hay)&&/(製造|工法|構造|材料|熱|温度|加熱|断熱|接合|洗浄|成形|組立|技術)/i.test(hay))return '製品・製造技術';if(CASE_RE.test(hay)&&localTopicRelevant(hay))return '改善事例';if(FOUNDATION_RE.test(hay)&&localTopicRelevant(hay))return '生技基礎';if(INNOVATION_RE.test(hay)&&localTopicRelevant(hay))return '技術革新';return '';}
function applicationIdea(raw={},category=''){const hay=`${raw.title||''} ${raw.summary||raw.description||''}`;if(category==='生技基礎')return '用語・計算式・使い分けを自工程の標準指標に置き換え、改善前後を同じ尺度で評価する基礎知識として使える。';if(/(OEE|設備総合効率|稼働率|可動率|チョコ停|停止時間|MTBF|MTTR)/i.test(hay))return '停止・速度・品質ロスを分解し、設備別/工程別KPI、ボトルネック、停止理由の標準化に転用できる。';if(/(トレーサビリティ|traceability|4M\b|ロット|工程履歴|検査履歴|部品情報)/i.test(hay))return 'Lot No.に設備・部品・作業・検査結果をひも付け、異常時の影響範囲特定と原因解析の短縮へ応用できる。';if(/(セル生産|コンベア|タクト|takt|サイクルタイム|line\s*balanc|ラインバランス|ボトルネック)/i.test(hay))return 'タクト差、待ち、滞留、作業者負荷を見える化し、ラインバランスや人員配置の改善に使える。';if(/(不良|歩留まり|直行率|FPY|工程能力|CpK?|ポカヨケ|検査)/i.test(hay))return '不良を4Mで分解し、直行率/歩留まりのKPI化、ポカヨケや検査方法の改善につなげられる。';if(category==='製品・製造技術')return '製品機能だけでなく、量産性、工程安定性、検査性、材料・設備条件へ分解して新規開発や工程設計の着眼点に使える。';if(category==='異業種横展開')return '業種固有の部分を外し、工程設計・設備・品質・データ活用の共通原理だけを自工程へ横展開できる。';return '手法・原理・定量効果を自工程の課題に置き換え、改善テーマや新規技術探索の着眼点として使える。';}
function normalizeSeed(seed={}){const title=clean(seed.title,320),url=safeUrl(seed.url),summary=clean(seed.summary,1200),sourceLabel=clean(seed.sourceLabel,140),organization=clean(seed.organization||sourceLabel,180);if(!title||!url||!summary||!sourceLabel||!organization||isBlockedUrl(url))return null;return{category:seed.category,title,url,summary,sourceLabel,organization,application:applicationIdea(seed,seed.category),pubDate:normalizeDate(seed.publishedAt),image:'',acquisition:'信頼できる公式解説・公式事例を直接収録',relevance:100,usefulness:100,selectionReason:'公式の基礎解説または具体的な製造改善事例として事前選定',topics:[]};}
function collectDirectSeeds(){return DIRECT_SEEDS.map(normalizeSeed).filter(Boolean);}
function normalizeDirectItem(raw={}, {category=''}={}){const title=clean(raw.title,320),url=safeUrl(raw.url||raw.link),summary=clean(raw.summary||raw.description||'',1200),sourceLabel=clean(raw.sourceLabel||raw.sourceName||raw.source||'',140),organization=clean(raw.organization||raw.author||sourceLabel,180),resolvedCategory=category||categoryOf(raw);if(!title||!url||!summary||!sourceLabel||!organization||!resolvedCategory||isBlockedUrl(url))return null;const hay=`${title} ${summary}`;if(PROMO_ONLY_RE.test(hay)&&!localTopicRelevant(hay))return null;if(!localTopicRelevant(hay))return null;return{category:resolvedCategory,title,url,summary,sourceLabel,organization,application:applicationIdea(raw,resolvedCategory),pubDate:normalizeDate(raw.publishedAt||raw.pubDate||raw.date),image:safeUrl(raw.image||raw.imageUrl),acquisition:clean(raw.acquisition||'公式RSS/APIによる直接取得',180),relevance:80,usefulness:70,selectionReason:'直接取得記事を技術関連判定で分類',topics:[]};}
function createQuietXmlDom(xml){const virtualConsole=new VirtualConsole();return new JSDOM(xml,{contentType:'text/xml',virtualConsole});}
function nodeText(node,selectors=[]){for(const selector of selectors){const value=node?.querySelector(selector)?.textContent?.trim();if(value)return value;}return '';}
async function searchJStage(term){const url=new URL('https://api.jstage.jst.go.jp/searchapi/do');url.searchParams.set('service','3');url.searchParams.set('text',term);url.searchParams.set('count','24');const response=await fetch(url,{headers:{Accept:'application/atom+xml,application/xml,text/xml','User-Agent':'PersonalDashboardTechnologyResearch/6.0'},signal:AbortSignal.timeout(12000)});if(!response.ok)throw new Error(`J-STAGE HTTP ${response.status} (${term})`);const dom=createQuietXmlDom(await response.text());try{return Array.from(dom.window.document.querySelectorAll('entry')).map(entry=>{const title=nodeText(entry,['article_title > ja','article_title > en','title']);const link=safeUrl(nodeText(entry,['article_link > ja','article_link > en'])||entry.querySelector('link')?.getAttribute('href')||nodeText(entry,['id']));const abstract=nodeText(entry,['abstract > ja','abstract > en','summary','description']);const journal=nodeText(entry,['material_title > ja','material_title > en']);const author=Array.from(entry.querySelectorAll('author > ja > name, author > en > name, author > name')).map(node=>clean(node.textContent,80)).filter(Boolean).slice(0,6).join(', ');const date=nodeText(entry,['pubdate','publication_date','published','online_pubdate','updated'])||nodeText(entry,['pubyear']);return normalizeDirectItem({title,url:link,summary:[abstract,journal&&`掲載誌: ${journal}`].filter(Boolean).join(' ｜ '),organization:author||journal||'J-STAGE',sourceLabel:'J-STAGE',publishedAt:date,acquisition:`J-STAGE WebAPI（検索軸: ${term}）`},{category:'論文・研究'});}).filter(Boolean);}finally{dom.window.close();}}
async function searchCiNii(term){const url=new URL('https://cir.nii.ac.jp/opensearch/articles');url.searchParams.set('q',term);url.searchParams.set('count','25');url.searchParams.set('format','rss');url.searchParams.set('lang','ja');const rows=await fetchRssSource({name:`CiNii Research:${term}`,url:url.href,maxItems:25});return rows.map(row=>normalizeDirectItem({title:row.title,url:row.link,summary:clean(row.description,1000),organization:row.author||'CiNii Research',sourceLabel:'CiNii Research',publishedAt:row.pubDate,image:row.image,acquisition:`CiNii Research OpenSearch（検索軸: ${term}）`},{category:'論文・研究'})).filter(Boolean);}
async function collectDirectPapers(){const jobs=[];for(const term of PAPER_QUERIES){jobs.push(searchJStage(term));jobs.push(searchCiNii(term));}const settled=await Promise.allSettled(jobs),items=[],errors=[];settled.forEach(result=>{if(result.status==='fulfilled')items.push(...result.value);else errors.push(String(result.reason?.message||result.reason||'paper source failed'));});return{items:dedupe(items),errors};}
async function collectMonoist(){try{const rows=await fetchRssSource({name:'MONOist',url:'https://rss.itmedia.co.jp/rss/2.0/monoist.xml',maxItems:100});const items=rows.map(row=>normalizeDirectItem({title:row.title,url:row.link,summary:row.description,organization:row.author||'MONOist',sourceLabel:'MONOist',publishedAt:row.pubDate,image:row.image,acquisition:'MONOist公式RSS'})).filter(Boolean);return{items,errors:[]};}catch(error){return{items:[],errors:[String(error?.message||error)]};}}
function isLikelyPromo(candidate={}){const hay=`${candidate.title||''} ${candidate.summary||''}`;return PROMO_ONLY_RE.test(hay)&&!/(改善|技術|工程|品質|設備|製造|分析|事例|原理|方法)/i.test(hay);}
function tavilyScore(raw={}){const rawScore=Number(raw?.score);if(!Number.isFinite(rawScore))return 0;return rawScore<=1?Math.max(0,rawScore)*100:scoreOf(rawScore);}
function categoryScore(category,text,rawScore,url=''){let score=rawScore*0.28,topicHits=0;if(PRODUCTION_RE.test(text)){score+=18;topicHits++;}if(QUALITY_RE.test(text)){score+=16;topicHits++;}if(EQUIPMENT_RE.test(text)){score+=16;topicHits++;}if(TRACE_RE.test(text)){score+=16;topicHits++;}if(PRODUCT_RE.test(text)){score+=16;topicHits++;}if(PRACTICAL_RE.test(text))score+=8;if(TRUSTED_PRIMARY_RE.test(url))score+=6;if(/(?:^|\.)note\.com$/i.test(hostOf(url)))score+=4;if(category==='生技基礎'){if(FOUNDATION_RE.test(text))score+=22;if(topicHits)score+=5;}else if(category==='改善事例'){if(CASE_RE.test(text))score+=22;if(EFFECT_RE.test(text))score+=8;}else if(category==='技術革新'){if(INNOVATION_RE.test(text))score+=24;if(PRACTICAL_RE.test(text))score+=4;}else if(category==='製品・製造技術'){if(PRODUCT_RE.test(text))score+=24;if(BRAND_RE.test(text))score+=6;}else if(category==='異業種横展開'){if(CROSS_INDUSTRY_RE.test(text))score+=24;if(CASE_RE.test(text))score+=8;}return Math.round(score);}
function acceptThreshold(category){if(category==='生技基礎')return 34;if(category==='技術革新'||category==='製品・製造技術')return 36;return 38;}
function topicsOf(text=''){const topics=[];if(PRODUCTION_RE.test(text))topics.push('生産技術');if(QUALITY_RE.test(text))topics.push('品質/QCD');if(EQUIPMENT_RE.test(text))topics.push('設備/OEE');if(TRACE_RE.test(text))topics.push('トレーサビリティ');if(FOUNDATION_RE.test(text))topics.push('基礎知識');if(CASE_RE.test(text))topics.push('改善事例');if(INNOVATION_RE.test(text))topics.push('新技術');if(PRODUCT_RE.test(text))topics.push('製品・製造技術');if(CROSS_INDUSTRY_RE.test(text))topics.push('異業種');return topics.slice(0,8);}
function sourceLabelOf(url=''){const host=hostOf(url);if(host==='note.com')return 'note';if(/itmedia\.co\.jp$/.test(host))return 'MONOist / ITmedia';return host||'Web';}
function normalizeTavilyResult(raw={},category){const title=clean(raw?.title,320),url=safeUrl(raw?.url),summary=clean(raw?.content||raw?.description||'',1400);if(!title||!url||!summary||isBlockedUrl(url))return{item:null,reason:'malformed'};if(isLikelyPromo({title,summary}))return{item:null,reason:'blocked'};const text=`${title} ${summary}`,searchScore=tavilyScore(raw),relevance=categoryScore(category,text,searchScore,url);if(relevance<acceptThreshold(category))return{item:null,reason:'rejected',score:relevance};const usefulSignals=[PRACTICAL_RE.test(text),FOUNDATION_RE.test(text),CASE_RE.test(text),INNOVATION_RE.test(text),PRODUCT_RE.test(text),EFFECT_RE.test(text)].filter(Boolean).length;const usefulness=Math.min(100,Math.round(45+usefulSignals*9+searchScore*0.12));const sourceLabel=sourceLabelOf(url),isNote=sourceLabel==='note';return{item:{category,title,url,summary,sourceLabel,organization:sourceLabel,application:applicationIdea({title,summary},category),pubDate:normalizeDate(raw?.published_date||raw?.publishedAt||raw?.date),image:safeUrl(raw?.image||''),acquisition:isNote?'Tavily Searchによるnote.com専用検索＋機械採点':'Tavily Searchによる一般Web検索＋機械採点',relevance:Math.min(100,relevance),usefulness,selectionReason:`検索関連度とカテゴリ語・実務語を加点して採用（score ${Math.min(100,relevance)}）`,topics:topicsOf(text)},reason:'accepted'};}
async function callTavily(plan,category){const apiKey=String(process.env.TAVILY_API_KEY||'').trim();if(!apiKey)throw new Error('TAVILY_API_KEY が設定されていません');const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),12000);try{const body={query:plan.query,topic:'general',search_depth:'basic',max_results:20,include_answer:false,include_raw_content:false,include_images:false,exclude_domains:['prtimes.jp','businesswire.com','prnewswire.com','makuake.com','camp-fire.jp','kickstarter.com']};if(Array.isArray(plan.includeDomains)&&plan.includeDomains.length)body.include_domains=plan.includeDomains;const response=await fetch('https://api.tavily.com/search',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${apiKey}`},body:JSON.stringify(body),signal:controller.signal});const data=await response.json().catch(()=>({}));if(!response.ok){const detail=data?.detail||data?.error||data?.message||`HTTP ${response.status}`;const error=new Error(`Tavily ${category}: ${clean(detail,180)}`);error.statusCode=response.status;throw error;}return{results:Array.isArray(data?.results)?data.results:[],noteOnly:Boolean(plan.noteOnly),query:plan.query};}finally{clearTimeout(timer);}}
async function collectWebResearch(){const jobs=[];for(const category of WEB_CATEGORIES)for(const plan of SEARCH_PLANS[category])jobs.push({category,plan,promise:callTavily(plan,category)});const settled=await Promise.allSettled(jobs.map(job=>job.promise)),items=[],errors=[],stats=Object.fromEntries(WEB_CATEGORIES.map(category=>[category,{requests:0,requestErrors:0,candidates:0,blocked:0,rejected:0,malformed:0,accepted:0,noteCandidates:0,noteAccepted:0}]));settled.forEach((row,index)=>{const{category}=jobs[index],stat=stats[category];stat.requests++;if(row.status==='rejected'){stat.requestErrors++;const message=clean(row.reason?.message||row.reason,180);errors.push(`${category}: ${message}`);return;}for(const raw of row.value.results){stat.candidates++;const url=safeUrl(raw?.url),isNote=/(?:^|\.)note\.com$/i.test(hostOf(url));if(isNote)stat.noteCandidates++;const normalized=normalizeTavilyResult(raw,category);if(!normalized.item){if(normalized.reason==='blocked')stat.blocked++;else if(normalized.reason==='rejected')stat.rejected++;else stat.malformed++;continue;}items.push(normalized.item);stat.accepted++;if(isNote)stat.noteAccepted++;}});for(const category of WEB_CATEGORIES)console.info(`[technology-research][${category}]`,stats[category]);return{items:dedupe(items),stats,errors,configured:Boolean(String(process.env.TAVILY_API_KEY||'').trim()),requestsPlanned:WEB_REQUESTS_PER_REFRESH,requestsSucceeded:settled.filter(row=>row.status==='fulfilled').length};}
function ageScore(item){const time=normalizeDate(item.pubDate).getTime();if(!time)return 0;const age=Math.max(0,(Date.now()-time)/86400000);if(age<=30)return 5;if(age<=90)return 4;if(age<=180)return 3;if(age<=365)return 2;if(age<=730)return 1;return 0;}
function sortScore(item){let score=scoreOf(item.relevance)+scoreOf(item.usefulness)*0.8+ageScore(item);if(TRUSTED_PRIMARY_RE.test(item.url||''))score+=5;if(/note\.com/i.test(item.url||''))score+=2;return score;}
function selectAllAccepted(items=[]){return dedupe(items).sort((a,b)=>sortScore(b)-sortScore(a)||normalizeDate(b.pubDate)-normalizeDate(a.pubDate));}
function toRssItems(items){return items.map(item=>({title:item.title,link:item.url,pubDate:item.pubDate.getTime()?item.pubDate:new Date(),author:item.organization,sourceName:`技術リサーチ｜${item.category}｜${item.sourceLabel}`,image:item.image,description:['技術リサーチ: Web調査済み',`研究方式: ${RESEARCH_VERSION}`,`対象企業/組織名: ${item.organization}`,`カテゴリ: ${item.category}`,`概要: ${item.summary}`,`応用着眼点: ${item.application}`,`媒体: ${item.sourceLabel}`,`関連度: ${Math.round(scoreOf(item.relevance))}`,`有用度: ${Math.round(scoreOf(item.usefulness))}`,item.selectionReason?`選別理由: ${item.selectionReason}`:'',item.topics?.length?`トピック: ${item.topics.join(', ')}`:'',`取得方式: ${item.acquisition}`].filter(Boolean).join(' ｜ ')}));}
function summarizeSourceCounts(items=[]){const counts={};items.forEach(item=>{const key=item.sourceLabel||'その他';counts[key]=(counts[key]||0)+1;});return Object.entries(counts).sort((a,b)=>b[1]-a[1]).slice(0,16).map(([source,count])=>`${source}:${count}`).join(', ');}
async function research({refresh=false}={}){if(!refresh&&memoryCache.xml&&Date.now()-memoryCache.at<CACHE_TTL)return memoryCache;const seeds=collectDirectSeeds();const[papers,monoist,web]=await Promise.all([collectDirectPapers(),collectMonoist(),collectWebResearch()]);const pool=dedupe([...seeds,...papers.items,...monoist.items,...web.items]),selected=selectAllAccepted(pool);if(!selected.length)throw new Error('条件を満たす技術リサーチ記事を取得できませんでした');const categoryCounts=Object.fromEntries(RESEARCH_CATEGORIES.map(category=>[category,selected.filter(item=>item.category===category).length])),sourceCounts=summarizeSourceCounts(selected),xml=rssXml('技術リサーチ',`Tavily Web検索＋note専用検索＋J-STAGE/CiNii/MONOist。機械採点に合格した記事は件数上限なしで全件配信。今回 ${selected.length}件。`,toRssItems(selected));const meta={count:selected.length,categoryCounts,sourceCounts,seedCount:seeds.length,paperErrors:papers.errors.length,monoistErrors:monoist.errors.length,webErrors:web.errors.length,webStats:web.stats,tavilyConfigured:web.configured,tavilyRequestsPlanned:web.requestsPlanned,tavilyRequestsSucceeded:web.requestsSucceeded};memoryCache.at=Date.now();memoryCache.xml=xml;memoryCache.items=selected;memoryCache.meta=meta;console.info('[technology-research] final',meta);return memoryCache;}
function categoryHeader(counts={}){return RESEARCH_CATEGORIES.map(category=>`${category}=${Number(counts[category]||0)}`).join(',');}
function candidateHeader(stats={}){return WEB_CATEGORIES.map(category=>{const row=stats[category]||{};return `${category}:${Number(row.candidates||0)}/${Number(row.accepted||0)}/note${Number(row.noteAccepted||0)}`;}).join(',');}
export async function technologyResearchFeed(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method Not Allowed'});const refresh=String(req.query?.refresh||'')==='1';try{const result=await research({refresh});res.setHeader('Content-Type','application/rss+xml; charset=utf-8');res.setHeader('Cache-Control',refresh?'no-store':'public, s-maxage=21600, stale-while-revalidate=43200');res.setHeader('X-Technology-Research-Count',String(result.items.length));res.setHeader('X-Technology-Research-Version','6');res.setHeader('X-Technology-Research-Seed-Count',String(result.meta?.seedCount||0));res.setHeader('X-Technology-Research-Paper-Errors',String(result.meta?.paperErrors||0));res.setHeader('X-Technology-Research-Monoist-Errors',String(result.meta?.monoistErrors||0));res.setHeader('X-Technology-Research-Web-Errors',String(result.meta?.webErrors||0));res.setHeader('X-Technology-Research-Tavily',result.meta?.tavilyConfigured?'configured':'missing-key');res.setHeader('X-Technology-Research-Tavily-Requests',`${Number(result.meta?.tavilyRequestsSucceeded||0)}/${Number(result.meta?.tavilyRequestsPlanned||WEB_REQUESTS_PER_REFRESH)}`);setAsciiHeader(res,'X-Technology-Research-Categories',categoryHeader(result.meta?.categoryCounts||{}));setAsciiHeader(res,'X-Technology-Research-Candidates',candidateHeader(result.meta?.webStats||{}));return res.status(200).send(result.xml);}catch(error){console.error('[technology-research]',error);if(memoryCache.xml){res.setHeader('Content-Type','application/rss+xml; charset=utf-8');res.setHeader('Cache-Control','no-store');res.setHeader('X-Technology-Research-Stale','1');return res.status(200).send(memoryCache.xml);}return res.status(502).json({error:'技術リサーチを取得できませんでした',detail:clean(error?.message||error,500)});}}
